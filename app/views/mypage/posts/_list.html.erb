<div id="my-posts" class="container mx-auto px-4 max-w-4xl">
  <div class="space-y-4 mt-4">
    <% if @posts.present? %>
      <%= render partial: 'posts/post', collection: @posts %>
    <% else %>
      <div class='text-lg text-center'>投稿したプリンはありません</div>
    <% end %>
  </div>
</div>