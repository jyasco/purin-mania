<div class="container mx-auto px-4  max-w-4xl">
  <div class="space-y-4">
    <% if @posts.present? %>
      <%= render @posts %>
    <% else %>
      <div class='text-lg text-center'>投稿はありません</div>
    <% end %>
  </div>
</div>