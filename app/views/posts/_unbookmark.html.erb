<%= link_to bookmark_path(current_user.bookmarks.find_by(post_id: post.id)), id: "unbookmark-button-for-post-#{post.id}", data: { turbo_method: :delete }, class: "text-accent hover:text-hover" do %>
  <i class="fas fa-bookmark text-[18px] sm:text-[20px]"></i>
<% end %>